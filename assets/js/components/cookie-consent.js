export function initCookieConsent(config){
  if(localStorage.getItem('verdeonCookieConsent:v1'))return;
  const dialog=document.createElement('section');dialog.className='cookie-consent';dialog.setAttribute('role','dialog');dialog.setAttribute('aria-label','Cookie preference');dialog.innerHTML=`<h3>Your privacy choice</h3><p>${config.cookie.text}</p><div class="cookie-consent__actions"><button class="v-button v-button--primary" type="button" data-cookie="accepted">${config.cookie.accept}</button><button class="v-button v-button--secondary" type="button" data-cookie="declined">${config.cookie.decline}</button><a class="cookie-consent__link" href="cookie-policy.html">${config.cookie.policyLabel}</a></div>`;document.body.append(dialog);requestAnimationFrame(()=>dialog.classList.add('is-visible'));dialog.querySelectorAll('[data-cookie]').forEach(button=>button.addEventListener('click',()=>{localStorage.setItem('verdeonCookieConsent:v1',button.dataset.cookie);dialog.remove()}));
}
