export function initAccordions(root=document){
  root.querySelectorAll('[data-accordion]').forEach(group=>{if(group.dataset.initialized)return;group.dataset.initialized='true';const buttons=[...group.querySelectorAll('.v-accordion__button')];const setState=(button,isOpen)=>{button.setAttribute('aria-expanded',String(isOpen));button.closest('.v-accordion__item')?.classList.toggle('is-open',isOpen);const panelId=button.getAttribute('aria-controls');const panel=panelId?document.getElementById(panelId):button.parentElement?.nextElementSibling;if(panel)panel.setAttribute('aria-hidden',String(!isOpen));const icon=button.querySelector('.v-accordion__icon');if(icon)icon.textContent=''};buttons.forEach(button=>setState(button,button.getAttribute('aria-expanded')==='true'));buttons.forEach(button=>{button.addEventListener('click',()=>{const expanded=button.getAttribute('aria-expanded')==='true';if(group.dataset.single==='true')buttons.forEach(other=>setState(other,false));setState(button,!expanded)})})});
}
